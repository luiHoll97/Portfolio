import React from "react"

// === Components ===
import NavBar from "../components/NavBar";

const Home = () : JSX.Element => {
  return (
    <div>
      <NavBar />
    </div>
  )
};

export default Home;
